import React from 'react'
import Select from 'react-select'

const CompetenceSelector = ({ competence }) => {
  return (
    <div>
        {competence.title}
    </div>
  )
}

export default CompetenceSelector